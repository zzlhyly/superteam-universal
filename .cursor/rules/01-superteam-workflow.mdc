---
description: "Superteam workflow phases and gate scripts for complex multi-step tasks requiring coordination across multiple specialists. Activated when working on superteam pipelines, multi-agent orchestration, or tasks spanning 3+ files."
alwaysApply: false
---

# Superteam Workflow

Use this workflow for complex tasks that span 3+ files or require multiple expertise areas.

## When to Use

- Feature spans 3+ files
- Requires multiple expertise areas (architecture, implementation, testing)
- Task can be decomposed into independent subtasks
- User explicitly requests multi-agent orchestration

## Workflow Phases

### Phase 1: PM (Requirements)
1. Explore codebase to understand existing patterns
2. Ask clarifying questions (max 5 at a time)
3. Generate `.superteam/spec.md` with acceptance gates
4. Present to user for approval

### Phase 2: Architect (Planning)
1. Read approved spec
2. Decompose into independent increments
3. Create frozen contracts with gate scripts
4. Generate `.superteam/plan.md`

### Phase 3: Execute (Implementation)
For each increment:
1. Generator implements per contract
2. Evaluator verifies with gates
3. If issues: revise and re-evaluate
4. If approved: proceed to next

### Phase 4: Evaluation (Verification)
1. Run ALL final acceptance gates
2. Binary PASS or FAIL
3. If FAIL: return to Phase 3 for fixes

### Phase 5: Delivery (Results)
1. Extract knowledge to wiki
2. Present results to user

## File Conflict Prevention

- Maintain a shared file lock list in `.superteam/file-locks.json`
- If two subtasks need the same file, run them sequentially
- Use dependency graph to determine order

## Gate Scripts

Create executable gate scripts in `.superteam/scripts/`:

```javascript
// gate-01-tests.js
const { execSync } = require('child_process');
try {
  execSync('npm test', { encoding: 'utf8', stdio: 'pipe' });
  console.log('PASS: All tests passing');
  process.exit(0);
} catch (error) {
  console.error('FAIL: Tests failed');
  process.exit(1);
}
```

## State Files

- `.superteam/state.json` - Pipeline state
- `.superteam/events.jsonl` - Event log
- `.superteam/spec.md` - Requirements
- `.superteam/plan.md` - Architecture plan
- `.superteam/contracts/` - Increment contracts
- `.superteam/verdicts/` - Evaluation verdicts
